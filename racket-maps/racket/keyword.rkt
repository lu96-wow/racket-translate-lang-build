;; racket/keyword
#lang racket/base
(provide plain-map re-exports)

;; re-exports (0)
(define re-exports '())

;; plain (1/1 translated)
(define plain-map (hash
  'keyword->immutable-string  "关键字->不可变-字符串"
))

