;; racket/load
#lang racket/base
(provide plain-map re-exports)

;; re-exports (0)
(define re-exports '())

;; plain (2/2 translated)
(define plain-map (hash
  '#%module-begin  "#%模块-开始"
  '#%top-interaction  "#%顶层-交互"
))

