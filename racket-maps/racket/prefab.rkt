;; racket/prefab
#lang racket/base
(provide plain-map re-exports)

;; re-exports (0)
(define re-exports '())

;; plain (2/2 translated)
(define plain-map (hash
  'immutable-prefab-struct-key  "不可变-预置-结构-键"
  'prefab-key-all-fields-immutable?  "预置-键-所有-字段-不可变?"
))

