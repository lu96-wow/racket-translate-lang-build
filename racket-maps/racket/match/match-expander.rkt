;; racket/match/match-expander
#lang racket/base
(provide plain-map re-exports)

;; re-exports (0)
(define re-exports '())

;; plain (1/1 translated)
(define plain-map (hash
  'define-match-expander  "定义-匹配-扩展器"
))

