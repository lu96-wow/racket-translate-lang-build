;; openssl/libcrypto
#lang racket/base
(provide plain-map re-exports)

;; re-exports (0)
(define re-exports '())

;; plain (3/3 translated)
(define plain-map (hash
  'libcrypto  "加密-库"
  'libcrypto-load-fail-reason  "加密-库-加载-失败-原因"
  'openssl-lib-versions  "OpenSSL-库-版本"
))

