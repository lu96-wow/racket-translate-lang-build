;; openssl/libssl
#lang racket/base
(provide plain-map re-exports)

;; re-exports (0)
(define re-exports '())

;; plain (2/2 translated)
(define plain-map (hash
  'libssl  "SSL-库"
  'libssl-load-fail-reason  "SSL-库-加载-失败-原因"
))

