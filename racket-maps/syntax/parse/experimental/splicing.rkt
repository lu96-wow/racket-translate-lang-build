;; syntax/parse/experimental/splicing
#lang racket/base
(provide plain-map re-exports)

;; re-exports (0)
(define re-exports '())

;; plain (0/1 translated)
(define plain-map (hash
  'define-primitive-splicing-syntax-class  #f
))

