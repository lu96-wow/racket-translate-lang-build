;; pkg/info
#lang racket/base
(provide plain-map re-exports)

;; re-exports (0)
(define re-exports '())

;; plain (2/2 translated)
(define plain-map (hash
  '#%info-domain  "#%信息-域"
  '#%info-lookup  "#%信息-查找"
))

