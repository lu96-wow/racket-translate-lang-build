;; net/url-connect
#lang racket/base
(provide plain-map re-exports)

;; re-exports (0)
(define re-exports '())

;; plain (4/4 translated)
(define plain-map (hash
  'current-connect-scheme  "当前-连接-方案"
  'current-https-protocol  "当前-HTTPS-协议"
  'tcp-abandon-port  "tcp-放弃-端口"
  'tcp-connect  "tcp-连接"
))

